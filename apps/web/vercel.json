{
    "buildCommand": "cd ../.. && npm run build --filter=web",
    "installCommand": "cd ../.. && npm install",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://webrtc-video-chat-app.onrender.com/:path*"
        }
    ]
}